GOOGLE_CREDENTIALS_PATH=<service_account_json_path>
SPREADSHEET_ID=<id_of_the_google_sheets_document>

# Optional. Log files in ingest/logs/ older than this are purged on every
# extraction run. Defaults to 14 if unset.
SHEETS_LOG_RETENTION_DAYS=14